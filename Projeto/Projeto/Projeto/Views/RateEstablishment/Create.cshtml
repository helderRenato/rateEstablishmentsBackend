@model Projeto.Models.EstablishmentRate

@{
   ViewData["Title"] = "Criar";
}

<h1>Avaliar o estabelecimento</h1>
<hr />

<div class="row">
   <div class="col-md-4">
      <form asp-action="Create" method="post">

         @* espaço para mostrar as mensagens de erro gerais  *@
         <div asp-validation-summary="ModelOnly" class="text-danger"></div>

         @*Selecionar quem é o utilizador a fazer a avaliação*@
         <div class="form-group">
             <label asp-for="User" class="control-label"></label>
             <select class="form-select" asp-for = "User">
                @foreach(var user in ViewData["Users"] as IEnumerable<User>)
                {
                    <option value="@user">@user.Username</option>
                }
            </select>
            <span asp-validation-for="User" class="text-danger"></span>
        </div>

         @*Selecionar quem é o utilizador a fazer a avaliação*@
         <div class="form-group">
             <label asp-for="Establishment" class="control-label"></label>
             <select class="form-select" asp-for = "Establishment">
                @foreach(var establishment in ViewData["Establishments"] as IEnumerable<Establishment>)
                {
                    <option value="@establishment">@establishment.Name</option>
                }
            </select>
            <span asp-validation-for="Establishment" class="text-danger"></span>
        </div>

         @*Selecionar quem é o estabelecimento a ser avaliado*@
         @*Comentar e Dar estrelas*@
         <div class="form-group">
             <label asp-for="Stars" class="control-label"></label>
            <select class="form-select" asp-for = "Stars">
              <option value="1">1</option>
              <option value="1.5">1.5</option>
              <option value="2">2</option>
              <option value="2.5">2.5</option>
              <option value="3">3</option>
              <option value="3.5">3.5</option>
              <option value="4">4</option>
              <option value="4.5">4.5</option>
              <option value="3">5</option>

            </select>
            <span asp-validation-for="Stars" class="text-danger"></span>
        </div>

         <div class="form-group">
            <label asp-for="Comment.Text" class="control-label"></label>
            <input asp-for="Comment.Text" class="form-control" />
            <span asp-validation-for="Comment.Text" class="text-danger"></span>
         </div>

         <div class="form-group">
            <input type="submit" value="Create" class="btn btn-primary" />
         </div>

      </form>
   </div>
</div>

<div>
   <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
   @{
      await Html.RenderPartialAsync("_ValidationScriptsPartial");
   }
}